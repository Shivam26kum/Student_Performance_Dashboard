import React from 'react';

type ToastPosition = 'top-left' | 'top-right' | 'top-center' | 'bottom-left' | 'bottom-right' | 'bottom-center';
type ToastAnimation = 'fade' | 'slide' | 'zoom' | 'bounce' | 'rotate' | 'flip' | 'swing';
type ToastType = 'default' | 'success' | 'error' | 'warning' | 'info';
type ToastTheme = 'colored' | 'light';
interface ToastCustomStyles {
    backgroundColor?: string;
    textColor?: string;
    fontSize?: string;
    borderRadius?: string;
    width?: string;
    height?: string;
    boxShadow?: string;
    border?: string;
    fontWeight?: string;
    padding?: string;
    fontStyle?: string;
    progressBarColor?: string;
    iconColor?: string;
}
interface ToastOptions {
    message: string;
    type?: ToastType;
    position?: ToastPosition;
    duration?: number;
    animation?: ToastAnimation;
    progressBar?: boolean;
    gradient?: [string, string];
    customStyles?: ToastCustomStyles;
    onClose?: () => void;
    theme?: ToastTheme;
    customIcon?: React.ReactNode | string;
    showIcon?: boolean;
    closePosition?: 'top' | 'inline';
}
interface Toast extends ToastOptions {
    id: string;
    isExiting?: boolean;
}
interface ToasterContextType {
    toasts: Toast[];
    notify: (options: ToastOptions) => string;
    removeToast: (id: string) => void;
}
declare const toaster: {
    notify: (options: ToastOptions) => string;
    removeToast: (id: string) => void;
};
declare const ToasterProvider: React.FC<{
    children: any;
}>;
declare const useToaster: () => ToasterContextType;

export { ToasterProvider, toaster, useToaster };
export type { Toast, ToastAnimation, ToastCustomStyles, ToastOptions, ToastPosition, ToastTheme, ToastType };
