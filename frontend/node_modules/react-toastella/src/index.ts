// index.ts

import { ToasterProvider, useToaster } from './contexts/ToasterContext';

// Exporting the provider and hook
export { ToasterProvider, useToaster };
export { toaster } from './contexts/ToasterContext';

// Export types/interfaces used by consumers of your library
export type {
  ToastPosition,
  ToastAnimation,
  ToastType,
  ToastTheme,
  ToastCustomStyles,
  ToastOptions,
  Toast,
} from './contexts/ToasterContext';
